\usepackage{
array, % help to define costum table columns
amssymb, % package for mathematical symbols and fonts
amsmath,
amsfonts,
booktabs, % beautify tables
color, % add colors
comment, % easy to add comments
caption, % customizing captions of figures, tables, and other floating environments.
cellspace, % additional spacing options for table cells
footmisc, % modify the appearance and behavior of footnotes
float, % enhanced functionality for handling floating objects, such as figures and tables
geometry, % customize the page layout and dimensions
makecell, % additional options and commands for formatting and customizing table cells.
pdflscape, % create landscape-oriented pages in a PDF document
ragged2e, % enhanced options for justifying and aligning text, in the table mostly
setspace, %  change the line spacing to single, one-and-a-half, or double spacing, or set a custom line spacing.
sectsty, % customize the formatting of sectioning commands such as chapter, section, subsection, etc.
subfigure, % divide a figure into multiple subfigures, each with its own caption and label
tabularx, % an extension to the standard "tabular" environment that allows you to create tables with adjustable column widths.
threeparttable, % adding table notes or table-wide footnotes to your tables.
ulem % provides various underlining styles and strikeout effects for text.
}

\usepackage[round]{natbib} % for standard citations
\usepackage[colorlinks=true, allcolors=blue]{hyperref} % change color of hyper reference
\usepackage[table,xcdraw]{xcolor} % colors

% set new geometry
\geometry{left=1.0in,right=1.0in,top=1.0in,bottom=1.0in}
\usepackage[final]{graphicx} % including and manipulating graphics

\usepackage[ruled, vlined, linesnumbered]{algorithm2e} % package to write algorithm
\usepackage{appendix} % package to write appendix
\usepackage{titlesec} % package to change the title of section, subsection, etc.
\newtheorem{proposition}{Proposition} % newtheorem to write proposition
\newtheorem{theorem}{Theorem} % newtheorem to write theorem
\usepackage{bm} % package to write bold math symbols
\renewcommand*\labelenumi{(\theenumi)} % change the label of enumerate
